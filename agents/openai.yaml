interface:
  display_name: "Working with ALMA Data"
  short_description: "Query, download, restore, and interpret ALMA data"
  default_prompt: "Use $working-with-alma-data to query, unpack, restore, or interpret this ALMA dataset safely."
